# AGENTS.md — 项目交接与 AI 协作约定

> 给 AI 看的"项目说明书"。每个新对话，第一句让 AI 读本文件 + PROGRESS.md。

## 项目简介
**九宫八阵 · 天命牌** —— 卡牌对战网页游戏，当前版本 **v2.0**（九宫格布阵 + 天机费用 + 魔将对战）。
纯静态网页游戏：**HTML + CSS + 原生 JavaScript**，无构建、无后端，双击 `index.html` 即可离线运行。

## 仓库结构
```
index.html               页面骨架（主战场 + 调试区 + 弹窗）
css/style.css            全局样式（暗金国风）
js/
├─ data/                 数据（五行/12英雄/八卦/64卦/魔王）
├─ core/                 核心（事件系统/对局状态/回合流转）
├─ systems/              系统（卦象合成/规则引擎/战斗）
├─ render/               渲染（英雄卡/战场/弹窗）
├─ input/                拖动攻击
├─ test/selftest.js      规则自检（514 项断言）
└─ main.js               入口与流程控制
tools/run-selftest.js    Node 环境自检运行器
tools/smoke-dom.js       DOM 冒烟测试
images/hero/<id>.png     英雄立绘（当前为程序生成占位图）
```

## 如何运行 / 测试
- 运行：双击 `index.html`（建议 Chrome / Edge）
- 规则自检：`node tools/run-selftest.js`（514 项断言：五行克制、八卦成卦、64 卦完整性、
  伤害公式取整、护盾/复活/持续伤害、魔王守护解锁、战斗集成、回合流转等）
- DOM 冒烟测试：`node tools/smoke-dom.js`
- ⚠️ **修改任何 JS/CSS/图片后，必须同步更新 `index.html` 与 `cardRenderer.js` 中的 `?v=` 版本号**，
  否则浏览器缓存会导致改了不生效

## 环境变量
无。纯静态项目，无后端、无密钥、无 `.env`。

## 开发约定
- 英雄立绘：`images/hero/<英雄id>/<皮肤id>.png`，建议 3:5 竖版（如 300×500）；缺失时降级为兵种图标文字
- 皮肤清单定义在 `js/data/heroes.js` 的 `skins` 字段（id / name / file）
- 改规则必须跑自检：`node tools/run-selftest.js`
- 核心玩法速记：上 3 下 2 共 5 格布阵；每回合 4 点天机，拖卡攻击耗 1 点；五行克制
  金→木→土→水→火→金（×1.3/×0.7）；暴击 10%；兵种特技 25%；开局三选一上卦，下卦由
  九宫格第 1 行前 3 格合成；5 魔将 + 本体「混沌·六爻魔」（100 血，魔将全灭前守护）

## 已踩过的坑（勿重复）
1. 巽卦爻序是「阴阳阳」（文档曾误写为「阴阴阳」与艮重复）
2. 64 卦特效 = 下卦规则 + 上卦规则自动组合；同键数值相加，概率类封顶 60%、减伤封顶 70%
3. 九宫格第 1 行空缺格按「阳」爻处理，不阻断成卦

## ⚠️ 铁律
1. 收工前必须更新 `PROGRESS.md`：做了什么 / 卡在哪 / 下一步 / 踩过的坑
2. 密钥、`.env`、构建产物永不提交
3. 每天至少 commit + push 一次
